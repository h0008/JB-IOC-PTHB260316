```mermaid
classDiagram
    class Computer {
        +calculatePrice(basePrice: double) double
        +calculatePrice(basePrice: double, tax: double) double
        +calculatePrice(basePrice: double, tax: double, discount: double) double
    }
```

